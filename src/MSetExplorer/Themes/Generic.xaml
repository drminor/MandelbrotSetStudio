<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:MSetExplorer">


    <!--<Style TargetType="{x:Type local:PanAndZoomControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:PanAndZoomControl}">
                    <Border BorderThickness="1" BorderBrush="Moccasin">

                        <ContentPresenter x:Name="PanAndZoomControl_Content">
                            <ContentPresenter.Content>

                                <Canvas x:Name="pAndZCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Transparent"/>

                            </ContentPresenter.Content>
                        </ContentPresenter>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->    
    
    
    <Style TargetType="{x:Type local:PanAndZoomControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:PanAndZoomControl}">
                    <Border BorderThickness="2" BorderBrush="DarkTurquoise">

                        <ContentPresenter x:Name="PART_Content" HorizontalAlignment="Center" VerticalAlignment="Center" />

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="Background">
            <Setter.Value>
                <SolidColorBrush Color="BlanchedAlmond"/>
            </Setter.Value>
        </Setter>
    </Style>    
    

    <!--<Style TargetType="{x:Type local:BitmapGridControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:BitmapGridControl}">
                    <Border BorderThickness="1" BorderBrush="DarkSlateBlue">

                        <ContentPresenter x:Name="BitmapGridControl_Content">
                            <ContentPresenter.Content>
                                <Canvas x:Name="bmgcCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Transparent">
                                    <Image x:Name="bmgcImage" />
                                </Canvas>

                            </ContentPresenter.Content>
                        </ContentPresenter>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->



    <!--<Style TargetType="{x:Type local:BitmapGridControl}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:BitmapGridControl}">
                    <Border BorderThickness="1" BorderBrush="ForestGreen">

                        <ContentPresenter x:Name="BitmapGridControl_Content">
                            <ContentPresenter.Content>
                                <Canvas x:Name="bmgcCanvas" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="Transparent">
                                    <Image x:Name="bmgcImage" />
                                </Canvas>

                            </ContentPresenter.Content>
                        </ContentPresenter>

                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>-->




</ResourceDictionary>