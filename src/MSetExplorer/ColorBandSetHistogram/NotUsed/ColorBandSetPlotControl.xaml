<UserControl x:Class="MSetExplorer.ColorBandSetPlotControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
                         xmlns:ScottPlot="clr-namespace:ScottPlot;assembly=ScottPlot.WPF"             
 
             xmlns:local="clr-namespace:MSetExplorer" d:DataContext="{d:DesignInstance Type=local:CbsHistogramViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border BorderThickness="0.75" BorderBrush="Black" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="35"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" BorderThickness="0.75" BorderBrush="Black">
                    <ScottPlot:WpfPlot x:Name="WpfPlot1"/>
                </Border>

                <Border Grid.Row="1" BorderThickness="0.75" BorderBrush="Black">
                    <Button Content="Press Me" Width="100" Click="Button_Click"/>
                </Border>


            </Grid>
        </Border>
            
    </Grid>
</UserControl>
